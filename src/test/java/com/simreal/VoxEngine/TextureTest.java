package com.simreal.VoxEngine;

public class TextureTest {

/*
TODO: Implement TextureTest
TODO: Implement DatabaseTest
TODO: Implement LightingTest
TODO: Implement NodePoolTest
TODO: Implement the various serialize/deserialize tests (if not in the above)

    double high = -9.0;
    double low = 9.0;
    double val;
    double lowx = 9.0;
    double highx = -9.0;
    double tx;
    int highb = 0;
    int lowb = 999;
    int b;
    Texture t = new Texture();
    t.transform = 0;
    t.inputScale = 10.0;
    for (double nx=0.0; nx<100.0; nx += 0.01) {
        for (double ny=0.0; ny<100.0; ny += 0.01) {
            val = t.noise(nx, ny);
            if (val < low) low = val;
            if (val > high) high = val;

            tx = t.xform(0, val);
            if (tx < lowx) lowx = tx;
            if (tx > highx) highx = tx;

            b = Texture.toByte(tx);
            if (b < lowb) lowb = b;
            if (b > highb) highb = b;
        }
    }
    System.out.println("NOISE RANGE: " + low + " .. " + high);
    System.out.println("TRANSFORM RANGE: " + lowx + " .. " + highx);
    System.out.println("BYTE RANGE: " + lowb + " .. " + highb);
*/
}
